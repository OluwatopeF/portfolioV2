{
  "homepage": "https://oluwatopeF.github.io/portfoiloV2",
  "scripts": {
  "dev": "next dev",
  "build": "next build && next export",
  "deploy": "npm run build && gh-pages -d out"
  },
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build && next export",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^15.3.2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
